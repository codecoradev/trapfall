//! TrapFall daemon — lightweight self-hosted error capture engine.

use anyhow::Result;

fn main() -> Result<()> {
    println!("trapfall v0.0.1");
    // TODO: Phase 0 — wire up all crates
    Ok(())
}
